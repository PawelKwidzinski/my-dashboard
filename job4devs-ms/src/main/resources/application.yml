spring:
  application:
    name: "job4devs"
  profiles:
    active: "dev"
  config:
    import: "optional:configserver:http://localhost:8888"

management:
  endpoints:
    web:
      exposure:
        include: "*"
server:
  port: 8080